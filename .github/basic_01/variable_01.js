const accountholderId =254254;

// We can not over write it.

let accountholderName = "Prince kashyap";

let accountholderCity = "Hathras";

let state

console.table([accountholderId,accountholderName,accountholderCity,state]);

// In present day we can not use var variable because of scope of block of code or scope functionality
